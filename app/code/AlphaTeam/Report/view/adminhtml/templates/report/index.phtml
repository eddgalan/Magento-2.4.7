<?php
/** @var $block Index */

use AlphaTeam\Report\Block\Adminhtml\CustomReport\Index;

$actionUrl = $block->getRunUrl();
$options   = $block->getReportTypeOptions();

/**
 * ToDo: Follow best practices and separate styles, HTML, and scripts
 */

?>

<style>
    .alphateam-report-loader {
        display: none;
        position: absolute;
        inset: 0;
        background: rgba(255,255,255,.7);
        justify-content: center;
        align-items: center;
        z-index: 100;
    }
    .alphateam-report-loader._active {
        display: flex;
    }
    .alphateam-spinner {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        border: 4px solid #ccc;
        border-top-color: #333;
        animation: alphateam-spin 1s linear infinite;
    }
    @keyframes alphateam-spin {
        to { transform: rotate(360deg); }
    }
</style>

<div class="alphateam-custom-reports-wrapper">
    <div class="alphateam-custom-reports">
        <form action="<?= $block->escapeUrl($actionUrl) ?>" method="post" class="alphateam-customreport-form">
            <?= $block->getBlockHtml('formkey'); ?>

            <div class="admin__field alphateam-report-row" style="display:flex;align-items:center;gap:20px;">
                <label class="admin__field-label" for="report_type" style="margin-bottom:0;">
                    <span><?= $block->escapeHtml(__('Select report')) ?></span>
                </label>

                <div class="admin__field-control" style="margin-bottom:0;">
                    <select name="report_type" id="report_type" class="admin__control-select">
                        <?php foreach ($options as $option): ?>
                            <option value="<?= $block->escapeHtmlAttr($option['value']) ?>">
                                <?= $block->escapeHtml($option['label']) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </div>

                <div class="admin__field-control" style="margin-bottom:0;margin-top:0;">
                    <button type="submit" class="action-default scalable">
                        <span><?= $block->escapeHtml(__('Generate Report')) ?></span>
                    </button>
                </div>
            </div>
        </form>
    </div>

    <div class="alphateam-report-loader">
        <div class="alphateam-spinner"></div>
    </div>
</div>
<script>
    require([
        'jquery'
    ], function ($) {
        'use strict';

        $(function () {
            const $form = $('.alphateam-customreport-form');
            const submitButton = $form.find('button[type="submit"]');

            $form.on('submit', function () {
                submitButton.attr('disabled', true);
            });
        });
    });
</script>
